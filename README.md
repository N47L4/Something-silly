Gumawa ka ng frontend natin html & css na lahat ng ito nakalagay:
(by 3 days dapat may mapakita na design)

- Login / Create Account Pages
  Design clean forms for username, password, and student ID (beep ID integration placeholder).
  Include error messages (e.g., “Invalid login” or “Account already exists”).
- Admin Dashboard (Data Entry)
  Create a form for entering book details (title, author, slot, summary).
  Add buttons for “Save,” “Update,” and “Delete.”
  Display confirmation messages after actions.
- Book Search & Reservation Interface
  Search bar with filters (title, author, category).
  Reservation button visible only for logged-in users.
  Confirmation popup when a reservation is successful.
- Slot Imagery / Availability Display
  Visual grid or list showing book status:
  Green = Available
  Red = Borrowed
  Include book summary preview when clicked.
- Borrower Account Page
  Show user’s borrowed books, due dates, and credit score.
  Highlight overdue books in red.
  Display penalties or restrictions if credit score is low.
- Notification UI
  Section for alerts (e.g., “Book overdue – check your email/SMS”).
  Simple design for reminders and warnings.
- General Styling
  Consistent layout across all pages.
  Use clear navigation (menu bar: Home, Search, Reservation, Account, Admin).
  Responsive design (works on desktop and mobile).


Frontend HTML Designer
- Build login/create account pages (forms, error messages).
- Design admin dashboard for book data entry.
- Create search & reservation interface with filters.
- Implement slot imagery (green = available, red = borrowed).
- Design borrower account page (borrowed books, due dates, credit score).
- Style notifications and alerts section.
- Ensure responsive, user-friendly design.

