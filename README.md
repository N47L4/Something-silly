Gawa ka ng flowchart ng each of the following:
(I need it beofre March 29, 2026 ends)

1.Login / Create Account System
- User opens system → decision (existing account?) → login or create → validate/save → access granted.
2.Data Entry (Admin Only)
- Admin logs in → enter book details (title, author, slot, summary) → save to database → confirmation.
3.Lending Item (Borrow Process)
- Student logs in with account + beep ID → search book → check availability → decision (available?) → borrow/reserve → update inventory (mark red if borrowed).
4.Credit Score Mechanism
- Borrow record updated → decision (returned on time?) → yes (no penalty) / no (deduct score) → update credit score → decision (score below threshold?) → restrict borrowing if needed.
5.Reservation System
- Student logs in → search book → check availability → decision (available?) → reserve slot → confirmation → update database.
6.Book Availability / Slot Imagery
- Book status updated → decision (borrowed?) → yes (mark red/unavailable) / no (mark green/available) → display real-time slot imagery.
7.Overdue Notification
- System checks due dates → decision (overdue?) → yes (send SMS/Gmail alert, deduct credit score) / no (no action) → update borrower record.
8.Borrower Record Management
- Student ID beep scan → fetch borrower details (name, contact, history) → update record with borrow/return → store permanently in database.

Flowchart Specialist / Tester
- Create flowcharts for all modules:
- Login/Create Account
- Data Entry (Admin)
- Lending Item
- Credit Score Mechanism
- Reservation System
- Slot Imagery (Availability)
- Overdue Notification
- Borrower Record Management
- Test each module for bugs and errors.
- Document test results and prepare user manual.
- Assist with presentation slides and demo.
